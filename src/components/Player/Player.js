import React from 'react';

const Player = ({ playerInfo }) => {
	return (
		<div />
		// <div>
		// 	{playerInfo.map(team => (
		// 		<div key={team.id}>
		// 			<div>{team.name}</div>
		// 			{/* <div>{team.city}</div> */}
		// 		</div>
		// 	))}
		// </div>
	);
};

export default Player;
